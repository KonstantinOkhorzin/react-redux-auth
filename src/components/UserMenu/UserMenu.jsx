import { Container } from './UserMenu.styled';

const UserMenu = () => {
  return (
    <Container>
      <p>Welcome, </p>
      <button>Log out</button>
    </Container>
  );
};

export default UserMenu;
